define(['deps/jasmine/jasmine-html', 'models/task'], function(jasmine, task) {
    with(jasmine) {
        describe("Hints", function() {

            it("", function() {
                expect(true).toBe(true);
                c.l(task)
            })
        })
        }
})
